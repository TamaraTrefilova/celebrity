$(function() {
  $("form").submit((event) {
  event.preventDefault();

  var inputName = $("input#name").val();
  var inputAge = $("input#age").val();
  var inputColor = $("input#color").val();

  if(inputAge === false || !inputName )


  });
});
